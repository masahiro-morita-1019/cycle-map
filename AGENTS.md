# cycle-map — Codex 運用メモ

このファイルは Codex が自動で読み込むプロジェクトメモリ。
人間と Codex の協働における取り決めを記す。

---

## Codex CLI との相互フィードバック

このプロジェクトでは、ローカルにある `codex` CLI を使って **独立した第二意見**
を取る運用を入れる。Codex が常に最良の判断をするとは限らないため、特定の
場面で Codex に相談・レビューを依頼することで品質と網羅性を高める。

### コマンド早見表

- 未コミットの変更をレビューさせる:
  ```bash
  codex review --uncommitted
  ```
- 自由なテーマで第二意見を取る (非対話):
  ```bash
  codex exec "<質問内容>"
  ```
- 直近のコミットをレビュー:
  ```bash
  codex review --commit HEAD
  ```

タイムアウトは Bash tool のデフォルト (2分) では足りないことがあるため、
`timeout: 600000` (10分) で実行する。

### Codex が自発的に Codex を呼ぶ場面

1. **大きめの実装をコミットする直前**
   - 目安: 新規ファイル 3 個以上、または 100 行以上の変更
   - `codex review --uncommitted` を実行 → 重要な指摘を要約してユーザーに共有してから commit

2. **アーキテクチャ判断で選択肢が割れたとき**
   - 例: 「ライブラリ A と B どちらが妥当か」「DB スキーマ X vs Y」
   - `codex exec "<状況と選択肢>"` で別視点を取り、Codex の推奨と並べてユーザーに提示

3. **デバッグが行き詰まったとき**
   - 目安: 同じ症状で 3 回以上の修正試行が失敗
   - 状況・試したこと・現状コードを `codex exec` に渡し、見落としがないか確認

4. **セキュリティ・性能が効く実装の直後**
   - 認証、SQL、外部 API 連携、Cron、ファイル I/O など
   - `codex review --uncommitted` で OWASP / レート制限 / リソース漏れ等の観点をチェック

### 呼ばない場面 (オーバーキル)

- 1 行修正・typo 修正・自明な依存上げ
- ドキュメントのみの変更
- 過去に同じ判断をした繰り返し作業

### 結果の扱い

- Codex の指摘は **参考意見** であり盲信しない
- Codex は指摘内容を自分で検証してから採否を決める
- 採用するもの・しないものを区別してユーザーに提示し、最終判断はユーザーに委ねる
- 明らかな事実誤認 (現状コードと矛盾する指摘等) はそのまま伝えず、Codex が訂正してから共有

### プロンプトの渡し方

- `codex exec` の引数は短く要点だけ。長い文脈はファイルパス参照で済ます
  (`codex exec "lib/gbfs/client.ts のリトライ戦略を見て、レート制限対策が
  十分か評価してほしい"` のように)
- レビュー依頼は **何を懸念しているか** を明示する。汎用的な「レビューして」
  より「N+1 クエリの有無を確認」のような focused な依頼の方が有用な返答に
  なる

---

## Claude Code との相互フィードバック

Codex からも、ローカルにある `claude` CLI (Claude Code) を使って **独立した
第二意見** を取る。Claude Code と Codex は得意・不得意や見落とし方が違うため、
実装・設計・レビューで判断が重い場面では Claude Code にも相談して品質を上げる。

### コマンド早見表

- 自由なテーマで第二意見を取る (非対話):
  ```bash
  claude -p "<質問内容>"
  ```
- 未コミットの変更をレビューさせる:
  ```bash
  claude -p "未コミット差分をレビューし、重大なバグ・設計リスク・不足テストに絞って指摘してください"
  ```
- 特定ファイルや論点を focused に見てもらう:
  ```bash
  claude -p "lib/gbfs/client.ts のリトライ戦略を見て、レート制限対策が十分か評価してください"
  ```

Claude Code の実行も時間がかかることがあるため、必要に応じて Bash tool の
`timeout: 600000` (10分) を使う。

### Codex が自発的に Claude Code を呼ぶ場面

1. **大きめの実装をコミットする直前**
   - 目安: 新規ファイル 3 個以上、または 100 行以上の変更
   - `claude -p "未コミット差分をレビュー..."` を実行し、重要な指摘を Codex が検証してからユーザーに共有する

2. **アーキテクチャ判断で選択肢が割れたとき**
   - 例: 「ライブラリ A と B どちらが妥当か」「DB スキーマ X vs Y」
   - `claude -p "<状況と選択肢>"` で別視点を取り、Codex の判断と並べてユーザーに提示する

3. **デバッグが行き詰まったとき**
   - 目安: 同じ症状で 3 回以上の修正試行が失敗
   - 症状・試したこと・関連ファイルを渡し、見落としや別アプローチがないか確認する

4. **セキュリティ・性能が効く実装の直後**
   - 認証、SQL、外部 API 連携、Cron、ファイル I/O など
   - OWASP、レート制限、権限境界、リソース漏れ、N+1 等の観点を明示してレビューを依頼する

### 呼ばない場面 (オーバーキル)

- 1 行修正・typo 修正・自明な依存上げ
- ドキュメントのみの変更
- 過去に同じ判断をした繰り返し作業
- Claude Code の起動や認証が壊れており、作業本体より復旧コストが高い軽微な変更

### 結果の扱い

- Claude Code の指摘は **参考意見** であり盲信しない
- Codex は指摘内容を現状コードで検証し、採用・不採用を判断する
- 採用するもの・しないものを区別してユーザーに提示し、必要なら最終判断をユーザーに委ねる
- 明らかな事実誤認 (現状コードと矛盾する指摘等) はそのまま伝えず、Codex が訂正してから共有する

### プロンプトの渡し方

- `claude -p` の引数は短く要点だけ。長い文脈はファイルパス参照で済ます
- 何を懸念しているかを明示する。汎用的な「レビューして」より、
  「N+1 クエリの有無を確認」「権限チェック漏れを確認」のような focused な依頼を優先する
- Claude Code に実装を任せる場合でも、Codex は差分を必ず確認し、ユーザーの未関連変更を巻き戻さない

---

## その他のプロジェクト規約

特になし (随時追記)。
